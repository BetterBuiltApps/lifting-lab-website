{
  "name": "lifting-lab-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "//screens": "Regenerates the WebP variants after adding or replacing a PNG in public/assets/screens/. Deliberately NOT part of `build`: the variants are committed, and the GitHub Actions runner has no cwebp.",
    "screens": "./scripts/build-screens.sh"
  },
  "dependencies": {
    "motion": "^12.43.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
