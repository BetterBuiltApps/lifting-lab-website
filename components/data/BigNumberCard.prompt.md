The result readout for every OWL calculator: 60px heavy amber number, unit on the baseline, caption beneath.

```jsx
<BigNumberCard number="347.2" caption="Sinclair total · 2021–2024 coefficients" />
<BigNumberCard number="182" unit="kg" caption="Estimated 1RM (Epley)" />
```

The caption is where OWL states data vintage or method — always say which formula or coefficient set produced the number.
