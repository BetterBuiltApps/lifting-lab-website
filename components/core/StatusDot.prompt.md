10px status dot for ratio diagnostics and readiness rows.

```jsx
<StatusDot status="caution" />
```

`none` is deliberately the decorative grey (35% white), Lifting Lab greys out a verdict it can't support rather than guessing.
