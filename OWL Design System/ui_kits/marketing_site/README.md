# OWL marketing site — UI kit

Pre-launch landing page: App Store CTA, dual hero (readiness check-in + bar
trace), four pillars, Fix My Miss, Meet Day, Progress & Badges, comparison
table, three-tier pricing, FAQ. Dark-first, real screenshots from `assets/screens/`.

## Before publishing

- `config.jsx` — `links.appStore` still points to `#app-store`; wire once the
  App Store listing exists.
- `config.jsx` — `links.support` is a placeholder email.

## Files

`config.jsx` config/copy constants · `Chrome.jsx` nav, section heading, shot
bezel, App Store button · `Hero.jsx` · `Problem.jsx` · `Pillars.jsx` (four
pillars, lead pillar = adaptive session builder) · `Spotlight.jsx` (Fix My
Miss + Meet Day) · `Progress.jsx` (levels, streaks, badges, share card) ·
`Comparison.jsx` · `Pricing.jsx` · `FAQFooter.jsx` (FAQ accordion + footer).
