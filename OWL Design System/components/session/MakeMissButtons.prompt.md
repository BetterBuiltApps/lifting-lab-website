The only way to log a set: a green Made and a red Miss, side by side, 56pt tall.

```jsx
<MakeMissButtons onMade={log} onMiss={promptMissCoach} />
```

Never shrink below 56pt or stack them vertically — the height exists because lifters tap these with chalk on their hands. A Miss opens the miss coach; a Made starts the rest timer.
