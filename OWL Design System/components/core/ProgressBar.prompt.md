Capsule progress bar with the amber→green gradient. Springs to its value with the house spring.

```jsx
<ProgressBar progress={0.92} height={6} />
```

`gradient="amber"` for pure-accent bars (program completion); the default amber→green reads as "progress toward good".
