Capsule chip, 12px heavy, color on a 16%-alpha tint of itself. Used for score grades (`A`), tracking quality, and phase tags.

```jsx
<Badge tone="amber">A</Badge>
<Badge tone="make">Good tracking</Badge>
```

Tints are always 16% alpha over the card, never a solid fill — solid fills are reserved for buttons.
