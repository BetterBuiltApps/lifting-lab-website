.DS_Store
node_modules/
site/dist/

# The real lockfile is site/package-lock.json. A root one is a stub from before
# site/ became the only npm workspace, nothing installs from the repo root.
/package-lock.json
